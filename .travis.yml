language: objective-c
cache: cocoapods
before_install:
  - sudo gem install cocoapods -v '0.39.0'  
script:
  xctool test -workspace Parking.xcworkspace -scheme Parking -sdk iphonesimulator -arch i386 -configuration Debug ONLY_ACTIVE_ARCH=NO
