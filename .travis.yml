branches:
 only:
  - master
git:
  depth: 5
language: objective-c
os: osx
osx_image: xcode7.3
before_install:
  - rvm install 2.2.5
  - xctool --version
  #- brew update
  #- brew outdated xctool || brew upgrade xctool
  - pod install
cache:
  - cocoapods
script:
  - xcodebuild -workspace Parking.xcworkspace -scheme Parking -sdk iphonesimulator test
